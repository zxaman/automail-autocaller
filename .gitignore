# Dependencies
node_modules/

# Environment and secrets
.env
.env.*
!.env.example

# Build output
build/
dist/
out/
coverage/
*.tsbuildinfo

# Logs
*.log
logs/

# OS/editor files
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local services and temporary files
.mongo/
.redis/
tmp/
uploads/

# Angular/Capacitor output (future)
.angular/
www/
android/
ios/

# Uploaded attachment bytes; these are user data, never source.
backend/storage/

# Capacitor native projects.
# `npx cap add android|ios` generates these from capacitor.config.ts and the
# web build. They are large, machine-specific, and regenerable, so they are
# not committed. Any custom native change must be re-applied deliberately.
frontend/android/
frontend/ios/
